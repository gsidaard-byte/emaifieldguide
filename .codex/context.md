# Codex Session Context

Start by reading `../context.md`.

This repo is the static EM x AI Field Guide site published at:

`https://gsidaard-byte.github.io/emaifieldguide/`

Main working file:

`../em-ai-walkthrough.html`

Important current state:

- The 18 NotebookLM MP4 files are already in `../notebooklm_videos/`.
- Videos are embedded through the `VIDEOS` map in `em-ai-walkthrough.html`.
- The video alignment fix is intentional: `.habit-video` should be full width with `width:100%` and no `max-width:640px`.
- `../Screenshot_Video_NotAligned.png` is an untracked reference screenshot and should not be committed unless asked.
